"use client";

const ButtonNoFill = ({ children }: any) => {
  return (
    <button className="border border-[#fcfc03] text-white text-lg py-2 px-4 rounded-lg">
      {children}
    </button>
  );
};

export default ButtonNoFill;
